import f from "../problems/1046-LastStoneWeight";

describe("main", () => {
  it("core", () => {
    // expect(f([2,7,4,1,8,1])).toBe(1);
    // expect(f([1])).toBe(1);
    expect(f([8,10,4])).toBe(2);
  });
});
